import { GetFullYearPipe } from './get-full-year.pipe';

describe('GetFullYearPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFullYearPipe();
    expect(pipe).toBeTruthy();
  });
});
