import { GetTotalDurationPipe } from './get-total-duration.pipe';

describe('GetTotalDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTotalDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
