import { CountriesEqualPipe } from './countries-equal.pipe';

describe('CountriesEqualPipe', () => {
  it('create an instance', () => {
    const pipe = new CountriesEqualPipe();
    expect(pipe).toBeTruthy();
  });
});
