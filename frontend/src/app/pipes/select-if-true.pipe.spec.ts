import { SelectIfTruePipe } from './select-if-true.pipe';

describe('SelectIfTruePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectIfTruePipe();
    expect(pipe).toBeTruthy();
  });
});
